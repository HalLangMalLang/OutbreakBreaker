#include "JMObjectPool.h"
#include "Modules/ModuleManager.h"

// 내가 만든 C++ 코드 묶음(모듈)을 언리얼 엔진에 등록
// FDefaultModuleImpl : 언리얼 엔진이 기본으로 제공하는 가장 일반적인 모듈 작동 방식
IMPLEMENT_MODULE(FDefaultModuleImpl, JMObjectPool);